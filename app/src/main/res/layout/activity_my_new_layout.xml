<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- TextView должен быть правильно закрыт -->
    <!-- ВАЖНО: здесь должен быть /> или ></TextView> -->

    <!-- Button должен быть правильно закрыт -->
    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="50dp"
        android:layout_marginBottom="30dp"
        android:gravity="center"
        android:text="Получать на почту рассылку"
        android:textColor="#2196F3"
        android:textSize="24sp"
        android:textStyle="bold" />

    <Button
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:paddingLeft="30dp"
        android:paddingTop="15dp"
        android:paddingRight="30dp"
        android:paddingBottom="15dp"
        android:text="Получить!"
        android:textSize="18sp" />  <!-- ВАЖНО: здесь должен быть /> или ></Button> -->

    <!-- Дополнительный TextView -->
    <!-- ВАЖНО: здесь должен быть /> -->

</LinearLayout>